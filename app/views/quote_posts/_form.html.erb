<%= form_for(@quote_post) do |f| %>
  <% if @quote_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote_post.errors.count, "error") %> prohibited this quote_post from being saved:</h2>

      <ul>
      <% @quote_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quote %><br>
    <%= f.text_area :quote %>
  </div>
  <div class="field">
    <%= f.label :author %><br>
    <%= f.text_field :author %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
